[package]
name = "hex-instset"
version = "0.1.0"
edition = "2021"

[dependencies]
hex-dump = { path = "../hex-dump" }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
