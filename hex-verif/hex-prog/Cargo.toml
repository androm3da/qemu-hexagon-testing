[package]
name = "hex-prog"
version = "0.1.0"
edition = "2021"

[dependencies]
hex-dump = { path = "../hex-dump" }
hex-instset = { path = "../hex-instset" }
hex-packet = { path = "../hex-packet" }
anyhow = { workspace = true }
askama = { workspace = true }
rand = { workspace = true }
toml = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
tempfile = "3"
